# Test Data

Reusable event, issue, PR, and git fixtures will live here as the GitHub-facing
commands are implemented. Current Phase 1 tests use inline fixtures for the
pure policy and branch planner behavior.
