# Config Fixtures

Repository-level Baton config examples and migration fixtures will live here as
implementation expands. Phase 1 currently keeps config coverage in Go unit
tests with inline fixtures.
